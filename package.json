{
  "name": "personelpro",
  "version": "2.0.0",
  "description": "Personel Yönetim Sistemi - Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute personelpro-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute personelpro-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.3.0",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.28.0"
  }
}